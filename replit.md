# بوت تيليجرام لتحميل يوتيوب

## نظرة عامة
بوت تيليجرام مكتوب بـ Python يستقبل روابط يوتيوب ويتيح للمستخدم تحميل الفيديو أو تحويله إلى MP3.

## الميزات
- يرد على أمر /start برسالة ترحيبية بالعربية
- يكشف روابط يوتيوب تلقائياً ويجلب معلومات الفيديو (العنوان، المدة)
- يعرض أزرار تفاعلية: تحميل فيديو أو تحميل صوت MP3
- يتحقق من حجم الملف (50 ميجابايت كحد أقصى)
- يحذف الملفات المؤقتة بعد الإرسال
- معالجة شاملة للأخطاء مع رسائل واضحة بالعربية

## البنية
- `main.py` — الكود الرئيسي للبوت (async)
- `pyproject.toml` — تبعيات Python

## التبعيات
### Python packages
- `python-telegram-bot` — مكتبة تيليجرام
- `yt-dlp` — تحميل فيديوهات يوتيوب
- `httpx` — طلبات HTTP

### System dependencies
- `ffmpeg` — تحويل الصوت إلى MP3

## المتغيرات البيئية
- `BOT_TOKEN` — توكن بوت تيليجرام (مطلوب، يُحفظ في Replit Secrets)

## تشغيل البوت
الـ workflow "Start application" يشغّل `python main.py` تلقائياً.
