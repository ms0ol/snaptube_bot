# بوت تيليجرام للتحميل متعدد المنصات

## نظرة عامة
بوت تيليجرام مكتوب بـ Python يدعم تحميل المحتوى من يوتيوب وتيك توك وإنستقرام وبينترست.

## الميزات
- /start: رسالة ترحيبية تعرض المنصات المدعومة
- كشف الرابط تلقائياً وتحديد المنصة
- يوتيوب: فيديو حتى 1080p أو تحويل إلى MP3
- تيك توك: تحميل بدون علامة مائية
- إنستقرام: ريلز وفيديوهات وصور
- بينترست: فيديوهات وصور
- أزرار تفاعلية حسب المنصة (فيديو / صوت)
- حد 50 ميجابايت مع رسائل واضحة عند التجاوز
- حذف الملفات المؤقتة تلقائياً
- معالجة شاملة للأخطاء بالعربية

## البنية
- `main.py` — الكود الرئيسي (async)
- `pyproject.toml` — تبعيات Python

## التبعيات
### Python packages
- `python-telegram-bot` — مكتبة تيليجرام
- `yt-dlp` — تحميل من جميع المنصات
- `httpx` — طلبات HTTP

### System dependencies
- `ffmpeg` — تحويل الصوت إلى MP3

## المتغيرات البيئية
- `BOT_TOKEN` — توكن بوت تيليجرام (مطلوب، يُحفظ في Replit Secrets)

## تشغيل البوت
الـ workflow "Start application" يشغّل `python main.py` تلقائياً.
